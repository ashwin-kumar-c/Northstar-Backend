# my-demo-app-backend

Simple Express backend for `my-demo-app`.

## Run

```sh
npm install
npm run dev
```

The server runs on `http://localhost:3001` by default.

## Endpoints

- `GET /api/message`
- `POST /api/greet` with JSON body `{ "name": "Ashwin" }`
